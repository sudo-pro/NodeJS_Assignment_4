<%-include('layouts/html-open.ejs')%>
    <%- include('layouts/container-open.ejs') %>
    <style>
        h1 {
            font-size: 3em;
            color: #3859ec; 
            animation: fadeIn 2s ease-in-out;
            margin: 60px 0 0 0;
        }
        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
        p {
            font-weight: bold;
            font-size: 1.5em;
            color: #f35c7d;
            margin-top: 20px;
            animation: slideIn 2s ease-in-out; 
        }
        @keyframes slideIn {
            from {
                opacity: 0;
                transform: translateX(-100%);
            }
            to {
                opacity: 1;
                transform: translateX(0);
            }
        }
    </style>
    <h1>Welcome</h1>
    <p>Assignment 4: User Management System with Express.js and Sequelize ORM</p>
    <%- include('layouts/container-close.ejs') %>
<%-include('layouts/html-close.ejs')%>
